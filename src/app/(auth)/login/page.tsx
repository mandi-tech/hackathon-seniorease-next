import FormLogin from "@/src/components/features/login/formLogin";

export default function LoginPage() {
  return (
    <section className="flex items-center justify-center h-screen">
      <FormLogin />
    </section>
  );
}
